contentdup.duplicate:
  path: /contentdup/{title}/{nid}
  defaults:
    _controller: \Drupal\contentdup\Controller\DupController::duplicate
    _title: Duplicate Node
  requirements:
    _permission: administer content
  options:
    parameters:
      title:
        type: 'string'
      nid:
        type: 'integer'